# Local-only context, not for the public repo
Coventry_Big_Data_Analytics_Coursework copy_New.pdf

# Secrets
.env
!.env.example

# OS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/
.python-version

# Datasets (large, download via scripts instead of committing)
*.csv
*.txt
!requirements.txt
data/
# Exception: Task 2's source is a live/growing Socrata dataset, not a fixed
# historical archive - re-running the fetch command later pulls a different
# "most recent 5000 rows", so the actual file is committed for reproducibility.
!task2-kafka-flink/data/
!task2-kafka-flink/data/camera_traffic_counts.csv
# Exception: Task 3's top-50 in-degree result is a tiny derived output (not
# the raw dataset), committed as evidence for the report.
!task3-spark/output/
!task3-spark/output/top50_indegree/
!task3-spark/output/top50_indegree/top50_indegree.csv

# Docker / infra volumes
**/influxdb-data/
**/neo4j-data/
**/spark-data/
**/grafana-data/

# Local-only scratch figures, not for the public repo
task4-neo4j/fig-all/

# Editors
.vscode/
.idea/
